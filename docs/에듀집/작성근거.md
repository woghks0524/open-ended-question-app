# 에듀집 서류 작성 근거 — AI 서술형 평가 도우미

방침(`public/privacy.html`)·체크리스트의 각 서술이 코드의 어느 동작에 근거하는지. 2026-09-04 작성.

| 방침 서술 | 코드 근거 |
|---|---|
| 회원가입·계정·쿠키 없음 (제2·3·12조) | 인증 코드 없음 — 로그인 라우트·세션·쿠키 설정 코드가 존재하지 않음. 유일한 브라우저 저장은 교사 접속 코드 localStorage (`src/lib/teacher-client.ts`) |
| 학생 학년·반·번호·이름은 DB에 저장 안 됨 (제2·5조) | `Step2StudentInfo` 입력은 클라이언트 state에만 있다가, 5단계 저장 시 `/api/results`가 교사 시트에 addRow (`src/app/api/results/route.ts` → `google-sheets.ts saveResults`). Supabase `assessments` 테이블에는 학생 컬럼 자체가 없음 (`supabase/schema.sql`) |
| 결과 저장은 학생이 선택한 경우에만 (제2·5조) | 5단계 `Step5Save`의 저장 버튼을 눌러야 `/api/results` 호출됨 |
| AI 채점에 학생 식별정보 미전송 (제2·9·10조) | `/api/grade`는 클라이언트에서 `{code, answers}`만 받고, OpenAI 입력 프롬프트에 이름·소속 미포함 (`src/app/api/grade/route.ts` input 구성부) |
| 모범답안·채점 기준·시트 주소를 학생 브라우저에 미전송 (제9조) | `/api/assessment` SENSITIVE_FIELDS 필터 (`src/app/api/assessment/route.ts`), 채점·저장은 서버가 code로 직접 조회 |
| 낮은 점수 피드백의 모범답안 노출 자동 차단 (제9조) | `hasAnswerLeak` + 교정 호출 (`src/app/api/grade/route.ts`), 2026-08-30~31 실측 0/53 |
| 교사 전용 기능 접속 코드 통제 (제9조) | `requireTeacher` (`src/lib/teacher-auth.ts`) — ⚠️ **2026-09-04 현재 개방 모드**(TEACHER_ACCESS_CODE 비움). **제출 전 재잠금 필요** — 방침 문구와 실상태를 일치시킬 것 |
| 학생 정보 로깅 없음 (제9조) | grade/results 라우트의 console 출력은 코드명·문항번호·오류만. 이름·답안 미기록 |
| 교과서 근거 채점·쪽수 안내 (제10조, 체크리스트 개요) | LIBRARY_VECTORSTORE_ID 110권 file_search + `page-ranges.ts` 쪽 범위 검증 |
| OpenAI API 학습 미사용 (제5·7조) | OpenAI API 데이터 정책 (기본 학습 미사용) — 사업자 정책 서술 |
| 위탁: Vercel/Supabase/OpenAI/Google (제7·8조) | 배포=Vercel(`.vercel/project.json`), DB=`src/lib/supabase.ts`, AI=`src/lib/openai.ts`, 이미지=`src/lib/firebase.ts`, 결과 기록=`src/lib/google-sheets.ts` |
| 트래킹·분석 도구 없음 (제3·12조) | 저장소 전체에 analytics/gtag/광고 스크립트 없음 |

## 제출 전 확인 사항

- [ ] **교사 접속 코드 재잠금** — 현재 개방 모드. 방침 제9조 "접속 코드로 접근을 통제합니다"와 일치시키려면 Vercel에 TEACHER_ACCESS_CODE 등록 후 재배포
- [ ] 개인정보 동의서 필요 여부 — 재환님이 직접 작성 관례. 필요하면 공식 HWP 양식 기반
- [ ] 구글 폼 제출은 재환님 직접
