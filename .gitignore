# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# 생각교실 정규화 결과(모범답안 포함) — 공개 저장소에 올리지 않음 (데이터는 구글 시트에 있음)
scripts/thinking-class-rows*.json
# typescript
*.tsbuildinfo
next-env.d.ts

# 생각교실 정규화 결과(모범답안 포함) — 공개 저장소에 올리지 않음 (데이터는 구글 시트에 있음)
scripts/thinking-class-rows*.json

# 모범답안 검사 중간·최종 산출물 — 문항과 모범답안 본문이 그대로 들어있다. 같은 이유로 제외.
# 스크립트로 다시 만들 수 있고, 저장소에는 본문을 뺀 model-answer-defects.json만 둔다.
scripts/model-answer-audit.json
scripts/model-answer-triage.json
scripts/model-answer-verified.json
scripts/model-answer-recheck.json
scripts/model-answer-full.json

# 보강안 산출물 — 모범답안 본문 포함, 위와 같은 이유로 제외
scripts/model-answer-drafts.json
scripts/model-answer-drafts.md
